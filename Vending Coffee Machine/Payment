
/**
 * Write a description of class Payment here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Payment
{
    public boolean processPayment(double total, double amountPaid)
    {
        if(amountPaid >= total){
            System.out.println("Pembayaran berhasil! Kembalian: Rp " + (amountPaid - total));
            return true;
        } else {
            System.out.println("Pembayaran gagal! Saldo anda tidak cukup, Anda perlu Rp " + (total - amountPaid) + " lagi");
            return false;
        }
    }
}
